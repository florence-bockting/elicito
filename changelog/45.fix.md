+ the default optimizer is usable again. It was stored as an optimizer
  instance, so a second run reused the state of the first one.
+ training now stops before a NAN gradient reaches the trainable variables, so
  the last valid values are kept.
+ a custom target function now receives only the arguments it declares.
+ initialization results are labelled from the initialization matrix, so every
  hyperparameter name matches its value.
+ a Pearson correlation of exactly `0.0` is no longer dropped. The correlation
  target failed with a reshape error whenever it occurred.
+ two `Elicit` objects no longer share one `meta_settings` dictionary.
+ eight error messages were raised as tuples and printed as `("...",)`. They
  are plain strings now.
