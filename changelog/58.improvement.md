Show the progress of the training and the initialization as a table.

The row shows the current loss, the best loss, the number of skipped steps,
the learning rate, the speed, the elapsed time and the remaining time.
CMA-ES and the initialization show the loss and the best loss only.
The table uses `rich`, so the `tqdm` dependency is removed.
